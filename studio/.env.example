# Sanity Studio — fill from sanity.io/manage → your project → API
# Mirror the frontend's NUXT_PUBLIC_SANITY_* values, just with the
# SANITY_STUDIO_ prefix (Sanity's own env-var convention).
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
